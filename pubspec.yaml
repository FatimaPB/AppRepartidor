name: repartidor
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0


environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  flutter_secure_storage: ^8.1.0
  geocoding: ^2.1.6
  google_maps_flutter: ^2.4.7   # ✅ Google Maps
  url_launcher: ^6.3.2          # ✅ Abrir Google Maps externo
  image_picker: ^1.0.4
  skeletonizer: ^2.1.0+1
  shimmer: ^3.0.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mockito: ^5.5.0
  build_runner: ^2.5.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/